// Revalidation interval for ISR (in seconds)
export const REVALIDATE_INTERVAL = 300 // revalidate every 5 minutes

// Query stale and garbage collection times (in milliseconds)
export const STALE_TIME = 1000 * 60 * 5 // 5 minutes
export const GC_TIME = 1000 * 60 * 60 * 24 // 24 hours 

// Currency formatting
export const CURRENCY_CODE = 'USD'
export const LOCALE = 'en-US'

// Debounce interval for search and input (in milliseconds)
export const DEBOUNCE_MS = 300;

